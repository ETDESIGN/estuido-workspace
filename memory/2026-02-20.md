# Memory Log - 2026-02-20 (Afternoon Session)

## QMD Fix & Memory System Hardening

### 4:00-5:00 PM - QMD Installation Verified & Fixed

**Initial State:**
- QMD reported as "broken" — npm package was just a stub (placeholder)
- Symlink pointing to /tmp/qmd/qmd (cleaned up, broken)
- User correctly questioned: "we installed qmd yesterday and you told me it was working"

**Root Cause:**
- `npm install https://github.com/tobi/qmd` only installed package.json stub
- Actual QMD is TypeScript project needing build step (bun + tsc)
- node-llama-cpp requires compilation (no prebuilt for this Linux system)

**Fix Applied:**
1. Cloned repo to /tmp/qmd-build
2. `bun install` — 358 packages
3. `bun run build` — compiled TypeScript
4. `bun link` — registered globally
5. Symlinked to ~/.local/bin/qmd

**Verification:**
```
qmd --version → qmd 1.0.8 (6ac7c68) ✅
```

**Indexing Results:**
- Collection: "estudio" 
- Files indexed: 158 markdown files
- BM25 search: ✅ Working
- Embeddings: 159 chunks from 88 documents (306.7 KB)
- Time: 2m 59s on CPU (no GPU)

---

## Key Workflow Patterns Identified

### Memory System Architecture (How I Remember)

**Current System:**
1. **Mandatory recall step** — I MUST run `memory_search` before answering about prior work
2. **Not automatic** — I actively search when relevant
3. **Two-step process:**
   - `memory_search` for semantic matches
   - `memory_get` for specific file lines
4. **Write path:** Daily logs → memory/YYYY-MM-DD.md, curated → MEMORY.md

**Tools Available:**
- `memory_search` — semantic search over all .md files
- `memory_get` — read specific file sections
- Native file-based (no QMD dependency for search)

### Cost Monitoring Pattern

**Daily Cost Check:** $0.41 / $5.00 threshold ✅
- Warren (COO) cron job monitors
- Alert if >$5 → switch to GLM-5 free tier
- Current strategy working (Ultra Low Cost mode)

### System Resource Constraints

**Hardware Reality:**
- **RAM:** 5GB total, 2.2GB available (after browsers, OpenClaw)
- **Swap:** 1.7GB used (performance warning)
- **Implication:** Cannot run large local models

**Decision Matrix — Local vs API:**
| Model | RAM Needed | Risk | Verdict |
|-------|-----------|------|---------|
| nomic-embed-text | ~300MB | Low | ✅ Safe |
| bge-m3 | ~1.1GB | Medium | ⚠️ Tight |
| qmd-query-expansion | ~1.2GB | High | ❌ Skip |

**Final Decision:** Use local embeddinggemma (314MB) for embeddings, skip query expansion model

---

## Issues Encountered & Solutions

### 1. QMD Build Issues
- **Problem:** node-llama-cpp no prebuilt binary for this Linux
- **Solution:** Compiled from source (cmake + gcc)
- **Time:** ~3 minutes first run
- **Status:** Working on CPU (slow but functional)

### 2. Session Timeouts
- **Problem:** Long-running operations (embed, build) timeout at 10s default
- **Pattern:** Use `process(action=poll, timeout=ms)` with appropriate timeouts
- **Lesson:** 60-120s timeouts for builds, 180s+ for embeddings

### 3. OpenRouter vs Local for Embeddings
- **User Request:** Use OpenRouter BAAI/bge-m3 for embeddings
- **Discovery:** QMD doesn't support OpenRouter for embeddings (local GGUF only)
- **Resolution:** Local embeddinggemma (314MB) sufficient and free
- **Cost Avoided:** ~$0.03/1M tokens (but unnecessary)

### 4. WhatsApp Gateway Instability
- **Pattern:** 408/428 errors, reconnecting every ~15 mins
- **Impact:** Notification delivery unreliable
- **Status:** Auto-reconnect working, no action needed

---

## Command Reference Established

### QMD Commands (Working)
```bash
# Search
qmd search "query"        # BM25 (fast, no model load)
qmd vsearch "query"       # Vector (needs query model - skip)
qmd query "query"         # Hybrid (needs query model - skip)

# Index Management
qmd collection add ~/.openclaw/workspace --name estudio --mask "**/*.md"
qmd embed                 # Generate embeddings
qmd status                # Check index health
qmd update                # Re-index collections

# Document Retrieval
qmd get "file.md:10" -l 20    # Get lines 10-30
qmd multi-get "pattern"       # Glob search
```

### Memory Scripts (Native)
```bash
./scripts/memory-search.sh "query"    # Semantic search
./scripts/memory-get.sh file.md       # Read file
./scripts/memory-save.sh "Title" "Content"  # Save decision
```

---

## User Preferences Observed

1. **Cost Sensitivity:** Prefers free tier, questions when costs might increase
2. **RAM Awareness:** Actively manages 5GB constraint, asks about memory usage
3. **Verification:** Questions status when contradictions appear (QMD working?)
4. **Documentation:** Wants patterns captured for incremental improvement
5. **Workflow Oriented:** Interested in systemizing operations

---

## Open Items (From TODO.md)

### 🔴 Still Pending
1. **Tavily Search Activation** — Brave Search rate limited
2. **Google Integration (gog skill)** — FREE alternative to Workspace
3. **Dashboard React Mount Issue** — Frontend crashes on load

### 🟡 Agent System
- Groq rate limits → use MiniMax/KiloCode for agents
- Tool request pipeline working (fs-watcher)

---

## Technical Debt / Future Improvements

1. **QMD Query Model:** 1.2GB model download pending (optional)
2. **Ollama:** Installed but not running (could enable for other tasks)
3. **CMake:** Downloads every QMD run (cache issue)
4. **Memory Pressure:** Chrome + WhatsApp using 3GB+ (consider alternatives)

---

*Session ended: 4:58 PM*
*Next: User review of memory capture, system improvements*
