# 2026-04-01 — Memory Log

## Stitch UI Designs Implemented & Deployed

### What Happened
Etia produced 4 Stitch (Google AI) design mockups and asked to implement them. First CTO subagent attempt via Kimi K2.5 failed (18s runtime, no output). Main agent implemented all 4 designs manually and deployed to Vercel.

### Stitch Designs Applied
1. **Profile Settings** — Hero card with gradient avatar ring, activity stats (border-left colored cards), personal info form (rounded-full inputs), Danger Zone with error styling
2. **Quote Comparison** — Side-by-side table with "Best"/"Lowest"/"Fastest" badges, alternating row backgrounds, sticky frosted-glass bottom action bar, supplier avatar stack
3. **Request Detail** — 7-stage pipeline stepper (horizontal pill bar), quote cards with "Best Price" highlight + green border, activity timeline with colored dots, compare mode toggle, sticky glass bottom bar
4. **Login Page** — Split layout: left panel with gradient branding + grid overlay + blur orb + 3 feature cards; right panel with rounded-full inputs, Material icon prefixes, password visibility toggle, gradient CTA

### Material Design 3 Theme Applied
- Full "Midnight Architect" color palette in `tailwind.config.js`:
  - Primary: `#adc6ff` (soft blue), Secondary: `#4edea3` (mint green), Tertiary: `#ffb95f` (amber)
  - Surface: `#0b1326` (deep navy), Surface Container: `#171f33`
  - Error: `#ffb4ab`, Outline: `#8c909f`
- Material Symbols Outlined font added to `globals.css` + Tailwind config
- Utility classes: `.glass-effect`, `.grid-overlay`, `.no-scrollbar`
- Border radius defaults: 1rem/2rem/3rem/full

### Files Modified
- `tailwind.config.js` — Complete MD3 color palette + font families + border radius
- `src/app/globals.css` — Material Symbols font import, utility classes, glass-effect
- `src/app/login/page.tsx` — Full Stitch redesign (split layout, Material icons, feature cards)
- `src/app/profile/page.tsx` — Full Stitch redesign (hero card, activity stats, danger zone)
- `src/app/requests/[id]/page.tsx` — Full Stitch redesign (pipeline stepper, quote cards, timeline, compare table, sticky bars)
- `src/components/layout/app-shell.tsx` — Material Symbols nav icons, active pill indicator, hover translate-x

### Build & Deploy
- Clean build ✅ (Tailwind v4 compatible — no `@layer base`, no `tw-animate-css`)
- Deployed to: `https://sourcing-dashboard-six.vercel.app`
- Commit: latest on `master` branch

### Subagent Failure Analysis
- **Kimi K2.5 CTO agent** ran only 18s, produced 524 tokens output, made zero file changes
- Likely cause: sandbox file path restrictions prevented reading project files
- Lesson: For complex file operations outside `~/.openclaw/workspace`, do the work directly rather than delegating

---

## Critical Technical Constraints (Reminder)
- **Tailwind v4**: Must use `@tailwind base; @tailwind components; @tailwind utilities;` — NO `@layer base`, NO `tw-animate-css`
- **next.config.js**: Must be `{ turbopack: {} }` only — NO `output: 'standalone'`
- **vercel.json**: outputDirectory set to `.next`
- **Proxy issues**: Mihomo TUN kills SSE streams (Google Stitch SDK unusable)
- **Sandbox**: Write tool limited to `~/.openclaw/workspace` — cannot write directly to `/home/e/sourcing-dashboard/`
- **Subagent model**: Must use `openrouter/moonshotai/kimi-k2.5` for critic/CTO spawns

---

## Git & Vercel
- Latest commits on `master` branch
- Vercel project: `prj_WT63ziGQ6P68eQ80JD0EcDdDDHRM`
- Deploy URL: `https://sourcing-dashboard-six.vercel.app`
- Turso DB: `libsql://turso-db-create-sourcing-estudio-remybrica.aws-ap-northeast-1.turso.io`

---

## Pending Items
- [ ] Stitch reference HTML files saved but not committed (not in repo, just reference)
- [ ] Quote Comparison page (`/requests/[id]?compare=true`) — partially implemented inline
- [ ] Order Tracking page
- [ ] Supplier Profiles page
- [ ] Messaging Center
- [ ] Reports & Analytics page
- [ ] Social Login integration
- [ ] Register API should respect `role` field from request body
- [ ] Register page redesign (Stitch only produced Login, not Register)
# 2026-04-01 — Memory Log

## Stitch UI Designs Implemented & Deployed

### What Happened
Etia produced 4 Stitch (Google AI) design mockups and asked to implement them. First CTO subagent attempt via Kimi K2.5 failed (18s runtime, no output). Main agent implemented all 4 designs manually and deployed to Vercel.

### Stitch Designs Applied
1. **Profile Settings** — Hero card with gradient avatar ring, activity stats (border-left colored cards), personal info form (rounded-full inputs), Danger Zone with error styling
2. **Quote Comparison** — Side-by-side table with "Best"/"Lowest"/"Fastest" badges, alternating row backgrounds, sticky frosted-glass bottom action bar, supplier avatar stack
3. **Request Detail** — 7-stage pipeline stepper (horizontal pill bar), quote cards with "Best Price" highlight + green border, activity timeline with colored dots, compare mode toggle, sticky glass bottom bar
4. **Login Page** — Split layout: left panel with gradient branding + grid overlay + blur orb + 3 feature cards; right panel with rounded-full inputs, Material icon prefixes, password visibility toggle, gradient CTA

### Material Design 3 Theme Applied
- Full "Midnight Architect" color palette in `tailwind.config.js`:
  - Primary: `#adc6ff` (soft blue), Secondary: `#4edea3` (mint green), Tertiary: `#ffb95f` (amber)
  - Surface: `#0b1326` (deep navy), Surface Container: `#171f33`
  - Error: `#ffb4ab`, Outline: `#8c909f`
- Material Symbols Outlined font added to `globals.css` + Tailwind config
- Utility classes: `.glass-effect`, `.grid-overlay`, `.no-scrollbar`
- Border radius defaults: 1rem/2rem/3rem/full

### Files Modified
- `tailwind.config.js` — Complete MD3 color palette + font families + border radius
- `src/app/globals.css` — Material Symbols font import, utility classes, glass-effect
- `src/app/login/page.tsx` — Full Stitch redesign (split layout, Material icons, feature cards)
- `src/app/profile/page.tsx` — Full Stitch redesign (hero card, activity stats, danger zone)
- `src/app/requests/[id]/page.tsx` — Full Stitch redesign (pipeline stepper, quote cards, timeline, compare table, sticky bars)
- `src/components/layout/app-shell.tsx` — Material Symbols nav icons, active pill indicator, hover translate-x

### Build & Deploy
- Clean build ✅ (Tailwind v4 compatible — no `@layer base`, no `tw-animate-css`)
- Deployed to: `https://sourcing-dashboard-six.vercel.app`
- Commit: latest on `master` branch

### Subagent Failure Analysis
- **Kimi K2.5 CTO agent** ran only 18s, produced 524 tokens output, made zero file changes
- Likely cause: sandbox file path restrictions prevented reading project files
- Lesson: For complex file operations outside `~/.openclaw/workspace`, do the work directly rather than delegating

---

## Critical Technical Constraints (Reminder)
- **Tailwind v4**: Must use `@tailwind base; @tailwind components; @tailwind utilities;` — NO `@layer base`, NO `tw-animate-css`
- **next.config.js**: Must be `{ turbopack: {} }` only — NO `output: 'standalone'`
- **vercel.json**: outputDirectory set to `.next`
- **Proxy issues**: Mihomo TUN kills SSE streams (Google Stitch SDK unusable)
- **Sandbox**: Write tool limited to `~/.openclaw/workspace` — cannot write directly to `/home/e/sourcing-dashboard/`
- **Subagent model**: Must use `openrouter/moonshotai/kimi-k2.5` for critic/CTO spawns

---

## Git & Vercel
- Latest commits on `master` branch
- Vercel project: `prj_WT63ziGQ6P68eQ80JD0EcDdDDHRM`
- Deploy URL: `https://sourcing-dashboard-six.vercel.app`
- Turso DB: `libsql://turso-db-create-sourcing-estudio-remybrica.aws-ap-northeast-1.turso.io`

---

## Project Spider — RFQ Email Sending (17:00-17:30 HKT)

### What Happened
Prepared 5 RFQ emails for battery charging dock suppliers. Attempted multiple sending methods — ALL failed due to Mihomo TUN proxy intercepting/blocking SMTP traffic at the network level.

### SMTP Failure Analysis
- **Gmail SMTP** (smtp.gmail.com:465/587) — SSL handshake fails, broken pipe. Mihomo TUN returns fake IPs in `198.18.0.0/15` range (virtual IP space)
- **Direct IP bypass** (172.217.221.108) — Still routed through Mihomo
- **Raw socket TLS** — Banner/EHLO/STARTTLS responses all empty (proxy MITM)
- **`no_proxy` env vars** — Ignored because Python's `urllib.request.getproxies()` picks up GNOME system proxy settings
- **Iptables bypass** — Mihomo uses TUN mode, not iptables NAT
- **mail.oookea.com:465/587/2525/25** — ALL timed out (same proxy issue)
- **Local postfix (port 25)** — Broken: no mailname, address resolver failure
- **Google OAuth API** — Token expired + no Gmail send scope
- **Playwright Brevo login** — Page load issues

### Etia Provided New SMTP Credentials
Etia created a custom domain email for sending:
- **SMTP Server:** `mail.oookea.com`
- **Port:** 465 (SSL), 2525 (non-SSL)
- **Username:** `dereck@oookea.com`
- **Password:** `Remybrica-1`
- **Domain:** oookea.com (hosted email)

### ⚠️ CORE BLOCKER REMAINS
Mihomo TUN proxy blocks ALL outbound SMTP regardless of server/credentials. The solution requires either:
1. Disabling Mihomo TUN temporarily
2. Adding mail.oookea.com (or the real IP) to Mihomo's bypass list
3. Using an HTTP email API (Resend, SendGrid, Brevo) instead of SMTP
4. Etia sending emails manually from his phone

### RFQ Email Files Ready
All 5 customized RFQs saved to `/home/e/.openclaw/workspace/memory/`:
- `spider-rfq-cheyoll.txt` → cheyoll06@cheyoll.com
- `spider-rfq-dudubox.txt` → hcj@dudubox.com
- `spider-rfq-chargenow.txt` → Info@chargenow.top
- `spider-rfq-heycharge.txt` → info@heycharge.global
- `spider-rfq-zhongdianhexin.txt` → WhatsApp +852-63569926

### Status: 0/5 RFQs sent — BLOCKED on network proxy

---

## Credentials to Store (Sensitive)
- `dereck@oookea.com` / `Remybrica-1` — SMTP on mail.oookea.com:465 (Etia's custom domain email)
- `caneles2hk@gmail.com` — Google Workspace (OAuth expired, no Gmail scope)
- Mihomo proxy: `http://127.0.0.1:7897` (HTTP), `socks://127.0.0.1:7897` (all)

---

## Pending Items
- [ ] **NEXT:** Try email skill or HTTP-based email API (Resend/SendGrid/Brevo) to bypass SMTP proxy issue
- [ ] Send 5 RFQ emails to Spider suppliers (0/5 sent)
- [ ] Stitch reference HTML files saved but not committed (not in repo, just reference)
- [ ] Quote Comparison page (`/requests/[id]?compare=true`) — partially implemented inline
- [ ] Order Tracking page
- [ ] Supplier Profiles page
- [ ] Messaging Center
- [ ] Reports & Analytics page
- [ ] Social Login integration
- [ ] Register API should respect `role` field from request body
- [ ] Register page redesign (Stitch only produced Login, not Register)
