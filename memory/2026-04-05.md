# 2026-04-05 Session Notes

## Nextcloud Recovery (oookea.qzz.io)
- **Domain:** https://oookea.qzz.io
- **Setup:** Snap Nextcloud v32.0.8 behind Cloudflare tunnel (`nextcloud`)
- **Tunnel config:** points to `localhost:80`
- **Fixed issues:**
  - System nginx was blocking port 80 → stopped and disabled it
  - Tailscale Funnel was blocking port 443 → turned it off
  - Snap Apache was crashing due to broken self-signed SSL certs forcing HTTPS redirect
  - Disabled SSL certs by moving them out of `/var/snap/nextcloud/current/certs/` and `/data/snap/nextcloud/common/current/certs/`
  - Now running HTTP only on port 80, Cloudflare handles SSL termination
- **Admin credentials:**
  - Username: `ET`
  - Password: `Remybrica-1` (reset via `sudo nextcloud.occ user:resetpassword ET`)
- **Cloudflare tunnel name:** `nextcloud`
