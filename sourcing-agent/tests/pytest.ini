[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = -v --tb=short
markers =
    smoke: quick smoke tests to run after deploy
    functional: page-by-page functional tests
    integration: cross-page integration tests
    performance: load / timing tests
